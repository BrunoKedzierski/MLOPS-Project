# ASI_Grupa3
## Dataset bank

1. Install conda from https://docs.conda.io/projects/miniconda/en/latest/

2. Open cmd and go to the catalog with this project inside.
conda env create -f environment.yml

4. Go to the asi-3 folder.

5. in cmd write command:
pip install -r src\requirements.txt

6. You can start the main pipiline by writing command below in cmd
kedro run
or you can open streamlit application by writing command below in cmd:
streamlit run streamlit_run.py

in streamlit you can change parameters and see the result of training.

